import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'PIX Transactions | Two X',
  description: 'This is PIX transactions page for admins',
  // other metadata
}

export default function PixTransactions() {
  return <div>PIX Transactions</div>
}
